# Este workflow está deshabilitado temporalmente
# El deploy a DreamHost se puede hacer manualmente via SFTP
# El GitHub Pages workflow (deploy.yml) sigue activo y funcionando correctamente

name: Deploy SFTP (Deshabilitado)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Workflow deshabilitado. Usa deploy.yml para GitHub Pages o sube manualmente a DreamHost"
